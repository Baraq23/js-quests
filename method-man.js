
const words = (s) => s.split(' ');
const sentence = (s) => s.join(' ');
const yell = (s) => s.toUpperCase();
const whisper = (s) => String('*' + s.toLowerCase() + '*');
const capitalize = (s) => {s.charAt(0).toUpperCase() + s.slice(1).toLowerCase()}; 
